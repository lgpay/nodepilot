# NodePilot v1.2.1

## 地图体验

- 使用更自然的 Natural Earth 世界投影，优化海洋、陆地和经纬网格层次。
- 节点标记显示名称，多节点位置自动聚合并显示数量。
- 支持滚轮缩放、拖拽平移、放大、缩小和重置视图。
- 节点标记随地图移动并保持清晰尺寸，继续支持点击打开节点详情。

## 验证

- Go 测试和前端 JavaScript 语法检查通过。
- Server / Agent 构建由 GitHub Actions 生成 Linux amd64 与 arm64 安装包。
